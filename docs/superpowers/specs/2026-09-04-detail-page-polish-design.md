# 상세페이지 다듬기 + 보태니컬 비누 리뉴얼 — 설계 스펙

날짜: 2026-09-04
승인: 사용자 (대화에서 A안 + 시나리오 v1 + 밀봉 훅/선물 섹션 절충안 승인)
배경: 온크트리 AI 상세페이지 대결 영상 벤치마크(위키 [[상세페이지 AI 냄새 신호]]) — 구조 층위 AI 냄새(번호 배지·구분선·넘버링)가 현행 파이프라인의 공백으로 확인됨.

## 목표

1. PRO 상세페이지 파이프라인에서 "AI가 만든 냄새"를 구조 층위에서 제거한다.
2. 개선된 파이프라인의 첫 실전 산출물로 보태니컬 비누(쿠팡 16202992314) 상세를 전면 리뉴얼한다.
3. 현행/개선 렌더 전후 비교본으로 개선 효과를 실측한다.

## 파이프라인 변경 5건

### 1. 선형 아이콘 라이브러리 (신규 `src/lib/detail-page/icon-library.ts`)

- Lucide(ISC) 서브셋 ~40종을 인라인 SVG 상수로 임베드. 규격: stroke 2px, `fill="none"`, `stroke="currentColor"`, viewBox 24×24.
- 분류: 계절/기간 · 재질/성분 · 세척/관리 · 보관 · 용량/구성 · 배송/포장 · 사용감(보습·향·거품·순함) · 의류(핏·기장·신축 — 자작).
- API: `getIcon(key: string, size?: number): string | null` — 유효하지 않은 키는 null.
- `generate-pro-layout` system-prompt DESIGN RULE 10 개정: icon 필드에 **라이브러리 키**를 넣도록 하고 사용 가능 키 목록을 프롬프트에 제공. 이모지는 계속 금지.
- `section-renderer.ts` `icon_grid`: 유효 키 → 선형 아이콘, 무효/빈 키 → 기존 번호 배지 폴백.
- `layout-validator.ts`: 무효 icon 키 warning (autoFix: 빈 문자열로 치환 → 폴백).

### 2. 구분선 제거 + 배경 리듬

- `section-renderer.ts` divider 블록: 선(border) 렌더를 수직 여백으로 대체.
- 섹션 배경 교대: 팔레트 기반 base → tint → base → dark accent(페이지당 1회) 로테이션. `wrap-rendered.ts` 또는 렌더러에서 섹션 컨테이너 background 지정.
- system-prompt에 divider 사용 억제 문구 추가.

### 3. 넘버링 나열 금지

- system-prompt: "첫 번째 고민/두 번째 고민" 류 순번 나열 금지 명문화.
- `narrative.ts`: 정규식 warning `(첫|두|세|네)\s*번째\s*(고민|장점|이유|문제)` 검출.

### 4. 훅 대형 타이포

- hero 헤드라인 clamp 상향. 390px 모바일 기준으로 실측 후 확정 (예: `clamp(26px, 7vw, 40px)`), 1.8배 스케일 규칙과의 상호작용 확인 필수.

### 5. 팔레트 → 씬 이미지 프롬프트 주입

- `generate-scene-image` 호출 시 palette-config의 주조색을 톤 힌트로 프롬프트에 추가. 미장센 규칙(앵글·조명)은 불변.

## 보태니컬 비누 시나리오 v2 (확정 — 2026-09-05 개정)

🔴 **v1에서 뒤집힌 것**: v1은 "선물 포장이 없다"는 전제로 밀봉을 훅에 놓고 선물을 뒤로 뺐다. **그 전제가 사실과 달랐다** — `/Volumes/Mac_SSD/Seller/보테니컬 비누/IMG_3755.JPG`에 창·마끈·"Thank you ESPECIALLY FOR YOU" 태그가 있는 기프트 박스 실물이 있고, 사용자가 **실제 배송 구성**임을 확인했다(2026-09-05). exec-guard의 "패키지와 매입 구조가 소구 축을 먼저 말한다"가 그대로 적용되어 **선물이 1축, 밀봉·대용량이 그 근거로 내려간다.**
(2026-08-23 이 사진의 썸네일 채택이 롤백된 사유는 **박스 상단 그림자라는 사진 결함**이지 구성 부정이 아니다 — [[보태니컬 비누 썸네일 교체 2026-08-23]] 7절. 재사용 시 상단 그림자를 지운다.)

제품 팩트 (코스트코 공식 p/635945, 2026-09-04 확인):
- 오스트레일리안 보태니컬 비누, 개당 **200g** (판매 구성 4개입 = 800g), 판매가 17,900원
- 향 4종("팩3"): 고트밀크 위드 소야빈 오일 · 피치스 앤 크림 · 릴리필리 위드 와틀씨드 익스트랙트 · 허니 앤 밀크
- 제조국 호주, 제조사 네츄럴 리소시즈 오스트레일리아, 수입 코스트코 코리아
- 사용기한 제조일로부터 36개월, **SLS 무첨가**(공식 표기), 모든 피부 타입
- 포장: 낱개 비닐 밀봉 + 창 있는 기프트 박스 + 마끈 + Thank you 태그
- 기존 detail0의 8개입 설명(팩2 향 목록)은 이번 리뉴얼로 해소

| # | beat | 내용 | 이미지 |
|---|---|---|---|
| 1 | hook | **"고맙다는 말, 손에 쥐어 드리세요"** — 기프트 박스 실물. 답례품·집들이·감사 선물 | IMG_3755 픽셀 보정(🔴 상단 그림자 제거 필수) |
| 2 | problem | 답례품 고르기의 고민 — 부담스럽지 않으면서 성의 있어 보여야 하고, 받는 사람 취향을 모른다 | 씬 1컷 |
| 3 | solution | 향 4종 구성 + 포장 완비 — 고르지 않아도 되고 따로 포장할 필요도 없다 | 박스 오픈 컷 |
| 4 | option | 4종 향 카드 그리드 (취향 문제의 해답이 여기서 닫힌다) | 향별 낱개 컷 4장 |
| 5 | evidence | 각인 선형 콜아웃: 개당 200g 대용량 · 호주산 · 시어버터 보습 · SLS 무첨가 | 각인 매크로 측면광 |
| 6 | compare | 좌: 한 봉지 멀티팩 / 우: 낱개 밀봉 + 기프트 박스 (포장 방식 비교) | compare_pair |
| 7 | usecase | **낱개 밀봉이라 하나씩 나눠도 새 상품 그대로** — 여러 명에게 나누는 답례품에 적합 | 낱개 밀봉 4종(IMG_3619) |
| 8 | usecase | 거품 사용감 — 뽀득하지만 당기지 않음, 온 가족. 향은 "은은한 편" 솔직 카피 | 손 거품 사용컷(캐릭터 시트) |
| 9 | care | 오래 쓰는 법 — 받침 + 조각 붙여 쓰기 | 받침 씬 |
| 10 | notice | 스펙 표(200g×4 · 4향 · 호주 · 36개월) + 고시 | HTML |

배경 리듬: 1 톤온톤 → 2–3 흰색 → 4 틴트 카드 → 5 흰색 → 6 틴트 → 7–8 실사/흰색 → 9–10 흰색. 구분선 0개.

카피 금지: 습진·아토피·트러블 개선 효능, 고트밀크 함량·"100% 천연", 향 과장, 트리플 밀드(국내 표기 없음). 카피 규칙 12개 + 구조 AI 냄새 신호 4종을 생성 프롬프트에 전문 주입.
🔴 v1의 "선물 포장 별도 없음 명시"는 **삭제** — 포장이 있으므로 불필요하며, 오히려 셀링포인트다.

검색태그·상품명: 답례품·집들이선물·감사선물 반영 검토 (sellerTags/searchTags 금지어 검증 후).

## 검증

1. 같은 레이아웃 JSON을 현행 렌더러/개선 렌더러로 각각 렌더 → 전후 비교본 파일 제공.
2. 모바일 폭(390px) 검수 + font-size 1.8배 규칙 적용 확인.
3. 산출물 사용자 검수 통과 후 채널 반영(쿠팡·네이버·토스 3채널 동기화 — 한 채널만 반영하는 실패 금지).
4. 결과를 위키 [[상세페이지 AI 냄새 신호]]에 실측 기록.

## 범위 밖

- REAL REVIEW 섹션(실제 리뷰 확보 전), 기존 타 상품 소급 재등록, GPT-5.6 대조 실험, FAQ 섹션(기존 판단 유지: 반박은 본문에서).
