export interface ShotCard {
  sectionTitle: string;
  subject: string;
  angle: string;
  framing: string;
  lighting: string;
  background: string;
  tip: string;
}

export interface ShotGuideInput {
  sectionTitle: string;
  promptHint: string;
}
